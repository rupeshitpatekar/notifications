apply plugin: "war"

bootWar {
    mainClassName = "com.invincible.notifications.ApiApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
